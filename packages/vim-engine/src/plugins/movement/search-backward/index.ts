/**
 * Search Backward Plugin Exports
 *
 * Exports the SearchBackwardPlugin for initiating backward search (? key).
 *
 * @example
 * ```typescript
 * import { SearchBackwardPlugin } from './search-backward';
 *
 * const searchInputManager = new SearchInputManager();
 * const plugin = new SearchBackwardPlugin(searchInputManager);
 * ```
 */

export { SearchBackwardPlugin } from './SearchBackwardPlugin';

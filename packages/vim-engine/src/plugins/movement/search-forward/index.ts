/**
 * Search Forward Plugin Exports
 *
 * Exports the SearchForwardPlugin for initiating forward search (/ key).
 *
 * @example
 * ```typescript
 * import { SearchForwardPlugin } from './search-forward';
 *
 * const searchInputManager = new SearchInputManager();
 * const plugin = new SearchForwardPlugin(searchInputManager);
 * ```
 */

export { SearchForwardPlugin } from './SearchForwardPlugin';
